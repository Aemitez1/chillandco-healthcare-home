# Token Efficiency Rules

- No scatter-shot exploration — know the target path before reading; use Grep/Glob for targeted search, not broad directory sweeps
- No trailing summary after task completion — the diff speaks for itself
- Read only files directly relevant to the current task
- Caveman mode active at full intensity (via startup hook)
