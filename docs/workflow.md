# Workflow Rules

## New Feature or Integration → Plan First

Before implementing any **new feature** or **integration** (new library, external API, third-party service):

1. Enter plan mode
2. Run `/grill-me` to resolve design decisions
3. Implement only after plan is approved

## Exceptions

Bug fixes, typo corrections, small isolated edits — skip plan mode, implement directly.
